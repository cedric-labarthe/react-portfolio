// Import styles
import './Navbar.css';

const Navbar = () => (
  <div className="navbar-container">
    <div>logo</div>
    <span>Profil</span>
    <span>Skills</span>
    <span>Projets</span>
    <span>Contacts</span>
  </div>
);

export default Navbar;
